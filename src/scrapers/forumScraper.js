/**
 * Forum ve Soru-Cevap Platformları Toplayıcısı
 * Sadece doğrulanmış canlı aramalar kullanılır.
 */

const forumScraper = {
  async scan() {
    return [];
  }
};

module.exports = forumScraper;
