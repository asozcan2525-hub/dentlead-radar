/**
 * Simülasyon Veri Akışı (Devre Dışı Bırakıldı)
 * Sistem artık yalnızca %100 gerçek internet kaynaklarını tarar.
 */

const simulationFeed = {
  getAllSamples() {
    return [];
  }
};

module.exports = simulationFeed;
